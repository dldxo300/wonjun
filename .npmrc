# Vercel 빌드 환경에서의 cross-device link 문제 해결
# symlinks=false

# isolated 링커 사용으로 cross-device link 문제 해결
node-linker=isolated

# pnpm 글로벌 스토어 비활성화 (파일시스템 간 하드링크 문제 해결)
store-dir=.pnpm-store
shared-workspace-lockfile=true
auto-install-peers=true

# 하드링크 대신 복사 사용 (성능은 떨어지지만 안정성 향상)
package-import-method=copy
